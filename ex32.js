// Create an array with 5 numbers.
// Ask the user for the length of the array you will like to print.
// Alert the user of the array from 0 to their input.



var SpecialNumbers = [ '2', '22', '8', '60','59'];
var len = prompt("Input the length of the array you will like to print");

for(var i=0; i<=len; i++) {
    console.log(i);
}
